# --- Required for app to work ---

# Neon (PostgreSQL). Get from: https://neon.tech dashboard → your project → Connection string
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Long random string for signing auth tokens (e.g. 32+ chars). Generate one: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# From https://platform.openai.com/api-keys (required for chat, embeddings, voice)
OPENAI_API_KEY="sk-your-openai-api-key"

# Optional: your deployed app URL (for links, etc.)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
